# Exchange-app
A simple and responsive web application built with React that allows users to convert one currency to another in real time. It uses the ExchangeRate API to fetch up-to-date exchange rates between multiple global currencies.
